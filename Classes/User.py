class User:
    def __init__(
            self,
            id,
            first_name,
            second_name,
            last_name,
            image,
            department,
            position,
            achievements

    ):
        self.id = id
        self.first_name = first_name,
        self.second_name = second_name,
        self.last_name = last_name,
        self.image = image,
        self.department = department,
        self.position = position,
        self.achievements = image


